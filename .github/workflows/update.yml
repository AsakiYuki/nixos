name: update flake inputs lock files

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check flake inputs
        uses: DeterminateSystems/flake-checker-action@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Check for update and perform update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          cd flakes/cursors && nix flake update && cd ../..
          cd flakes/overlays && nix flake update && cd ../..
          nix flake update

          git add flake.lock flakes/overlays/flake.lock

          if ! git diff --cached --quiet; then
            git commit -m "ci(flake.lock): update flake inputs $(date "+%H:%M:%S %d-%m-%Y")"
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "No changes in lock files to commit."
          fi

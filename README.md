# How to Build

Run `./build.sh` followed by one of the supported system targets:

```sh
# Format:
./build.sh <nixos-system>

# Examples:
./build.sh wsl # For Windows Subsystem for Linux
./build.sh ideapad-slim-5 # For Lenovo IdeaPad Slim 5
./build.sh home-server # For Home Server
```
